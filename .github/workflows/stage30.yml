name: EGHM maintenance
on:
  workflow_dispatch:
jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow temporarily paused while the six-observation calibration contract is updated."
